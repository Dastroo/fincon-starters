eu:
  fincon:
    starter:
      number: 999

spring:
  datasource:
    url: jdbc:postgresql://localhost/test
    username: postgres
    password: postgres
  jpa:
    database:
      platform: org.hibernate.dialect.PostgreSQLDialect